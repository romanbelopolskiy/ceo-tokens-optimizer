const payload = {
  hookSpecificOutput: {
    hookEventName: "SessionStart",
    additionalContext:
      "CEO Tokens Optimizer active. Keep work scoped, use narrow reads and targeted queries, write long deliverables to files, and summarize only the decisions, changed files, and verification.",
  },
};

console.log(JSON.stringify(payload));
